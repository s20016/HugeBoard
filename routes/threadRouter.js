const express = require('express');
const router = express.Router();
const threadModel = require('../controllers/threadController')

router.get('/new', );
router.get('/:slug', )
router.post('/', );
router.delete('/:slug/new/:id', );

module.exports = router;

